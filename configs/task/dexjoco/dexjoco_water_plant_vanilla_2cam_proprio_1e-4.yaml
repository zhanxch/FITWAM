# @package _global_

defaults:
- override /data: dexjoco_water_plant_2cam_success
- override /model: fastwam
- _self_
output_dir: ${oc.env:FITWAM_OUTPUT_ROOT,runs}/${wandb.name}/${now:%Y-%m-%d_%H-%M-%S}
batch_size: 4
num_workers: 4
model:
  proprio_dim: ${data.train.processor.proprio_output_dim}
  state_dit_config: null
  skip_dit_load_from_pretrain: true
  action_dit_pretrained_path: null
lr_scheduler_type: cosine
learning_rate: 0.0001
num_epochs: 5
max_steps: 6500
log_every: 10
save_every: 500
eval_every: 500
gradient_accumulation_steps: 2
weight_decay: 0.01
resume: ${oc.env:FASTWAM_RESUME,null}
wandb:
  enabled: true
  workspace: null
  project: fitwam
  name: dexjoco_water_plant_vanilla_2cam_proprio_1e-4
  group: dexjoco_water_plant_failure_ablation
  mode: online
