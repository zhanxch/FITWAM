# @package _global_

defaults:
  - override /data: egovla_pour_balls_clean_1s_2s
  - override /model: fastwam
  - _self_

batch_size: 8
num_workers: 8

model:
  mot_checkpoint_mixed_attn: false

lr_scheduler_type: "cosine"
learning_rate: 1e-4
num_epochs: 150
max_steps: null
log_every: 10
save_every: 5000
eval_every: 1000

gradient_accumulation_steps: 2
weight_decay: 1e-2
resume: null
