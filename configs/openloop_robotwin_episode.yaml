# @package _global_

defaults:
  - openloop_robotwin
  - _self_

OPENLOOP:
  # 0-indexed within the selected split after the train/val split is applied.
  episode_index: 0
  episode_indices: null
  frame_stride: 50
  max_samples: 8
  output_dir: ./evaluate_results/robotwin_openloop_episode/${hydra:runtime.choices.task}/${now:%Y%m%d_%H%M%S}
