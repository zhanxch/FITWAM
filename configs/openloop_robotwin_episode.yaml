# @package _global_
# Deprecated historical name. Use `openloop_episode.yaml` for new runs.

defaults:
  - openloop_episode
  - _self_

OPENLOOP:
  output_dir: ./evaluate_results/robotwin_openloop_episode/${hydra:runtime.choices.task}/${now:%Y%m%d_%H%M%S}
